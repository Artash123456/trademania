import styled from 'styled-components';
const ChartLoading = () => <StyledLoading className="loading-anim" />;

const StyledLoading = styled.div`
  grid-area: chart;
  min-height: 200px;
`;
export default ChartLoading;
